---
title: jsonValue
description: Creates a JSON value schema.
source: /schemas/jsonValue/jsonValue.ts
contributors:
  - fabian-hiller
---

import { ApiList, Property } from '~/components';
import { properties } from './properties';

# jsonValue

Creates a JSON value schema.

```ts
const Schema = v.jsonValue<TMessage>(message);
```

## Generics

- `TMessage` <Property {...properties.TMessage} />

## Parameters

- `message` <Property {...properties.message} />

### Explanation

With `jsonValue` you can validate that the input is a value that can be serialized and deserialized with `JSON.stringify` and `JSON.parse` without loss. This includes strings, numbers, booleans, `null`, and objects or arrays that recursively contain only these types. If the input is not a JSON value, you can use `message` to customize the error message.

## Returns

- `Schema` <Property {...properties.Schema} />

## Examples

The following examples show how `jsonValue` can be used.

### JSON value schema

Schema to validate any JSON-serializable value.

```ts
const JsonSchema = v.jsonValue();
```

### JSON value with custom message

Schema to validate a JSON value with a custom error message.

```ts
const JsonSchema = v.jsonValue('This is not a valid JSON value.');
```

## Related

The following APIs can be combined with `jsonValue`.

### Schemas

<ApiList
  items={[
    'array',
    'exactOptional',
    'intersect',
    'lazy',
    'looseObject',
    'looseTuple',
    'map',
    'nonNullable',
    'nonNullish',
    'nonOptional',
    'nullable',
    'nullish',
    'object',
    'objectWithRest',
    'optional',
    'record',
    'set',
    'strictObject',
    'strictTuple',
    'tuple',
    'tupleWithRest',
    'undefinedable',
    'union',
  ]}
/>

### Methods

<ApiList
  items={[
    'assert',
    'config',
    'fallback',
    'getDefault',
    'getDefaults',
    'getFallback',
    'getFallbacks',
    'is',
    'message',
    'parse',
    'parser',
    'pipe',
    'safeParse',
    'safeParser',
  ]}
/>
