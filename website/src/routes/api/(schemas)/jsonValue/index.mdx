---
title: jsonValue
description: Creates a JSON value schema.
source: /schemas/jsonValue/jsonValue.ts
contributors:
  - ksaurav24
---

import { ApiList, Property } from '~/components';
import { properties } from './properties';

# jsonValue

Creates a JSON value schema.

```ts
const Schema = v.jsonValue<TMessage>(message);
```

## Generics

- `TMessage` <Property {...properties.TMessage} />

## Parameters

- `message` <Property {...properties.message} />

### Explanation

With `jsonValue` you can validate that the input matches strings, finite numbers, booleans, `null`, and objects or arrays that recursively contain only these types. It is validation-only: on success, the input is returned unchanged rather than copied into a new array or object, so `__proto__`, `prototype`, and `constructor` are treated like any other key when they occur as an object's own property, and no data is silently dropped or altered. Because the input is not copied, mutating the returned value also mutates the original input value. Values with custom `toJSON` behavior (for example `Date`) are not specially handled; their own enumerable properties are used instead, the same way the `record` schema treats them (unlike `object`, which reads off the keys declared in its entries rather than the input's own keys), so such a value that passes validation is returned as the original instance rather than a plain object. An object or array that references itself, directly or through a nested value, is rejected with an issue instead of being followed. Also note that very deeply nested input can exceed the call stack, so untrusted input should have its depth bounded before parsing. If the input is not a JSON value, you can use `message` to customize the error message.

## Returns

- `Schema` <Property {...properties.Schema} />

## Examples

The following examples show how `jsonValue` can be used.

### JSON value schema

Schema to validate any JSON-serializable value.

```ts
const JsonSchema = v.jsonValue();
```

### JSON value with custom message

Schema to validate a JSON value with a custom error message.

```ts
const JsonSchema = v.jsonValue('This is not a valid JSON value.');
```

## Related

The following APIs can be combined with `jsonValue`.

### Schemas

<ApiList
  items={[
    'array',
    'exactOptional',
    'intersect',
    'lazy',
    'looseObject',
    'looseTuple',
    'map',
    'nonNullable',
    'nonNullish',
    'nonOptional',
    'nullable',
    'nullish',
    'object',
    'objectWithRest',
    'optional',
    'record',
    'set',
    'strictObject',
    'strictTuple',
    'tuple',
    'tupleWithRest',
    'undefinedable',
    'union',
  ]}
/>

### Methods

<ApiList
  items={[
    'assert',
    'config',
    'fallback',
    'getDefault',
    'getDefaults',
    'getFallback',
    'getFallbacks',
    'is',
    'message',
    'parse',
    'parser',
    'pipe',
    'safeParse',
    'safeParser',
  ]}
/>
